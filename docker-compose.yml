version: "3.9"

services:

  app:
    build: .
    container_name: contractiq-api
    ports:
      - "8080:8080"
    env_file:
      - .env
    volumes:
      - ./data:/app/data
      - ./faiss_index:/app/faiss_index
    command: uvicorn api.main:app --host 0.0.0.0 --port 8080 --reload
